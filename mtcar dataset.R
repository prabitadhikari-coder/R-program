data(mtcars)
str(mtcars)
summary(mtcars)
head(mtcars)
tail(mtcars)
#View(mtcars)

#Descriptive
mean(mtcars $mpg)
median(mtcars $mpg)
sd(mtcars $mpg)
sum(mtcars $mpg)
var(mtcars $mpg)
min(mtcars $mpg)
max(mtcars $mpg)

#Simple visualization
hist(mtcars$cyl)
hist(mtcars$hp)
colnames(mtcars)


